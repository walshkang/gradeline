assignment_id: "assignment5"
scoring_mode: "equal_weights"
partial_credit: 0.5

bands:
  check_plus_min: 0.85
  check_min: 0.67

questions:
  - id: "1"
    label_patterns: ["1)", "1.", "(1)"]
    anchor_tokens: ["1)", "1.", "(1)"]
    scoring_rules: >
      Student should correctly state the linear regression equation as 
      T = 3.080 + 0.08756F, along with the R-squared value of 32.1% 
      and the standard error of estimate Se = 0.966.
    short_note_pass: "Correct."
    short_note_fail: "Needs revision."
    weight: 1.0

  - id: "2"
    label_patterns: ["2)", "2.", "(2)"]
    anchor_tokens: ["2)", "2.", "(2)"]
    scoring_rules: >
      Student should correctly state the linear regression equation as 
      T = 31.5464 - 18.651R, along with the R-squared value of 65.4% 
      and the standard error of estimate Se = 0.690.
    short_note_pass: "Correct."
    short_note_fail: "Needs revision."
    weight: 1.0

  - id: "3"
    label_patterns: ["3)", "3.", "(3)"]
    anchor_tokens: ["3)", "3.", "(3)"]
    scoring_rules: >
      Student should correctly state the multiple linear regression equation as 
      T = 21.890 + 0.07214F - 17.211R, along with the R-squared value of 86.7% 
      and the standard error of estimate Se = 0.450.
    short_note_pass: "Correct."
    short_note_fail: "Needs revision."
    weight: 1.0

  - id: "4"
    label_patterns: ["4)", "4.", "(4)"]
    anchor_tokens: ["4)", "4.", "(4)"]
    scoring_rules: >
      Student should conclude that the best regression equation is Regression III 
      because the F ratio (29.44) and all T ratios (T1 = 3.81, T2 = -6.09) are 
      statistically significant. Crucially, the student must explain that simply 
      having the highest R-squared or lowest standard error is insufficient justification, 
      as the T tests determine if adding a variable increases the explanatory power by a 
      statistically significant amount.
    short_note_pass: "Correct."
    short_note_fail: "Needs revision."
    weight: 1.0